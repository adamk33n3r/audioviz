import { Effect } from './effect';

export class {{pascalCase name}}Effect extends Effect {
    public process(buffer: AudioBuffer): AudioBuffer {
        throw new Error('Method not implemented.');
    }
}
